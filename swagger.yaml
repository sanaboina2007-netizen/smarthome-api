openapi: 3.0.0
info:
  title: SmartHome API (Minimal)
  version: 1.0.0

servers:
  - url: https://sanaboina2007-netizen.github.io/smarthome-api

paths:
  /ping:
    get:
      summary: Health check
      responses:
        '200':
          description: SmartHome API is running
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
